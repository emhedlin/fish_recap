"""Main pipeline orchestration module.

This module coordinates the preprocessing pipeline (segmentation, metric extraction,
standardization) and the matching engine (feature extraction, matching, geometric
verification) to process raw fish images and perform re-identification.
"""

